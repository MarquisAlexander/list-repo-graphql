import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  repoTitle: {
    fontSize: 21,
    lineHeight: 31,
    fontFamily: 'Poppins-SemiBold',
    marginTop: 16,
    marginBottom: 16,
    color: '#32C0C6',
  },
});
